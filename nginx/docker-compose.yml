services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
      target: php
    container_name: my-php-fpm
    volumes:
      - ./html:/var/www/html
    extra_hosts:
      - "host.docker.internal:host-gateway"

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    container_name: my-nginx
    ports:
      - "80:80"
    volumes:
      - /mnt/raid_1/html:/var/www/html:ro
      - /mnt/raid_1/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - php
